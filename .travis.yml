language: node_js
node_js:
    - "8"
before_install:
    - npm install -g grunt-cli
    - npm install -g bower
    - npm install -g npm
    - bower install
    - npm update
    - bower update
notifications:
    email:
        on_success: change
        on_failure: change
sudo: false
